{
  "name": "@merveil/machine-connect-core",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && jest --config jest.config.cjs --runInBand",
    "audit:modules": "node audit-modules.mjs"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-fastify": "^11.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "mqtt": "^5.14.1",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.0.0",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.5",
    "typescript": "^5.7.0"
  }
}
